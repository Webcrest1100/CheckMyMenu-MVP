// const express = require("express");
// const router = express.Router();
// const upload = require("../middleware/multer");
// const protect = require("../middleware/authMiddleware");
// const { uploadLogo, getLogo, deleteLogo } = require("../controllers/logoController");

// // ✅ Upload logo
// router.put("/:id", protect, upload.single("logo"), uploadLogo);

// // ✅ Get logo
// router.get("/:id", getLogo);

// // ✅ DELETE logo — THIS MUST EXIST!
// router.delete("/:id", protect, deleteLogo);

// module.exports = router;
